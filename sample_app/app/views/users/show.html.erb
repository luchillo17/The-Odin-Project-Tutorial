<div class="row">
	<aside class="col-md-4">
		<section class="user_info">
			<h1>
				<%= gravatar_for @user %>
				<div>
					<strong>Name:</strong>
					<%= @user.name %><br>
					<strong>Email:</strong>
					<%= @user.email %>
				</div>
			</h1>
		</section>
	</aside>
</div>

<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
